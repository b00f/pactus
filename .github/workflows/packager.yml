name: Packager
permissions:
  contents: read
  id-token: write

on:
  workflow_run:
    workflows: ["Releaser"]
    types: ["completed"]

jobs:
  packager:
    runs-on: ubuntu-latest

    steps:
      - name: Print workflow_run event
        run: echo "${{ toJson(github.event.workflow_run) }}"
